@model HuellitasFelices.Models.Consulta
@{
    ViewData["Title"] = "Proceder al pago";
    var totalMed = Model.Medicamentos?.Sum(m => m.Subtotal) ?? 0;
    var totalGeneral = Model.Costo + totalMed;
}

<div class="page-header"><div class="container">
    <nav aria-label="breadcrumb"><ol class="breadcrumb mb-2">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Inicio</a></li>
        <li class="breadcrumb-item"><a asp-controller="Account" asp-action="MiPanel">Mi panel</a></li>
        <li class="breadcrumb-item active">Pago</li>
    </ol></nav>
    <h1><i class="bi bi-credit-card me-2"></i>Proceder al pago</h1>
</div></div>

<div class="content-wrapper"><div class="container">
<div class="row justify-content-center"><div class="col-lg-7">

    <!-- Factura resumen -->
    <div class="hf-card mb-4">
        <div class="hf-card-header"><i class="bi bi-receipt me-2"></i>Resumen de la consulta</div>
        <div class="card-body p-4">
            <div class="d-flex justify-content-between mb-2">
                <span><i class="bi bi-patch-heart me-1"></i>Mascota</span>
                <strong>@Model.Mascota?.Nombre</strong>
            </div>
            <div class="d-flex justify-content-between mb-2">
                <span><i class="bi bi-person-badge me-1"></i>Veterinario</span>
                <span>@Model.Veterinario?.Nombre</span>
            </div>
            <div class="d-flex justify-content-between mb-2">
                <span><i class="bi bi-chat-left-text me-1"></i>Motivo</span>
                <span>@Model.Motivo</span>
            </div>
            <div class="d-flex justify-content-between mb-2">
                <span><i class="bi bi-clipboard2-check me-1"></i>Diagnostico</span>
                <span>@(Model.Diagnostico ?? "Sin diagnostico")</span>
            </div>
            <div class="d-flex justify-content-between mb-2">
                <span><i class="bi bi-calendar3 me-1"></i>Fecha</span>
                <span>@Model.FechaConsulta.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</span>
            </div>

            <hr style="border-color: var(--border);" />

            <div class="d-flex justify-content-between mb-2">
                <span>Costo de consulta</span>
                <strong>$@Model.Costo.ToString("F2")</strong>
            </div>

            @if (Model.Medicamentos != null && Model.Medicamentos.Any())
            {
                <div class="mt-3">
                    <strong><i class="bi bi-capsule me-1"></i>Medicamentos recetados:</strong>
                    <div class="table-responsive mt-2">
                        <table class="hf-table">
                            <thead><tr><th>Producto</th><th>Cant.</th><th>Precio</th><th>Subtotal</th></tr></thead>
                            <tbody>
                                @foreach (var med in Model.Medicamentos)
                                {
                                    <tr>
                                        <td>
                                            <strong>@med.Producto?.Nombre</strong>
                                            @if (!string.IsNullOrEmpty(med.Dosis))
                                            {
                                                <br /><small class="text-muted">Dosis: @med.Dosis</small>
                                            }
                                            @if (!string.IsNullOrEmpty(med.Indicaciones))
                                            {
                                                <br /><small class="text-muted">@med.Indicaciones</small>
                                            }
                                        </td>
                                        <td>@med.Cantidad</td>
                                        <td>$@med.PrecioUnitario.ToString("F2")</td>
                                        <td><strong>$@med.Subtotal.ToString("F2")</strong></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="d-flex justify-content-between mb-2 mt-2">
                        <span>Subtotal medicamentos</span>
                        <strong>$@totalMed.ToString("F2")</strong>
                    </div>
                </div>
            }

            <hr style="border-color: var(--border);" />

            <div class="d-flex justify-content-between" style="font-size:1.15rem;">
                <span class="fw-bold">Total a pagar</span>
                <span class="fw-bold" style="color: var(--primary);">$@totalGeneral.ToString("F2")</span>
            </div>
        </div>
    </div>

    <!-- Formulario de pago -->
    <div class="hf-card">
        <div class="card-body p-4">
            <form asp-action="PagarConfirm" method="post" id="formPagoConsulta">
                @Html.AntiForgeryToken()
                <input type="hidden" name="consultaId" value="@Model.Id" />
                <input type="hidden" name="metodoPago" value="" id="metodoPagoHidden" />

                <div class="text-center mb-4">
                    <div style="font-size:.9rem;color:var(--text-soft);">Total a pagar</div>
                    <div style="font-size:2.2rem;font-weight:800;color:var(--primary);">$@totalGeneral.ToString("F2")</div>
                </div>

                <div style="max-width:440px;margin:0 auto;">
                    <button type="submit" onclick="document.getElementById('metodoPagoHidden').value='PayPal'"
                            style="display:flex;align-items:center;justify-content:center;gap:10px;width:100%;padding:16px;border:none;border-radius:24px;background:#FFC439;cursor:pointer;transition:all .15s;margin-bottom:12px;font-family:Arial,sans-serif;"
                            onmouseover="this.style.background='#f0b92e'" onmouseout="this.style.background='#FFC439'">
                        <span style="font-weight:800;font-size:1.2rem;color:#003087;font-style:italic;">Pay</span><span style="font-weight:800;font-size:1.2rem;color:#0070ba;font-style:italic;">Pal</span>
                    </button>

                    <button type="submit" onclick="document.getElementById('metodoPagoHidden').value='PayPhone'"
                            style="display:flex;align-items:center;justify-content:center;gap:10px;width:100%;padding:16px;border:none;border-radius:20px;background:#FF6B00;cursor:pointer;transition:all .15s;font-family:Arial,sans-serif;"
                            onmouseover="this.style.background='#e55e00'" onmouseout="this.style.background='#FF6B00'">
                        <i class="bi bi-phone-fill" style="color:#fff;font-size:1.1rem;"></i>
                        <span style="font-weight:700;font-size:1rem;color:#fff;">PayPhone</span>
                    </button>
                </div>
            </form>

            <div class="text-center mt-4">
                <a asp-controller="Account" asp-action="MiPanel" style="font-size:.85rem;color:var(--text-soft);text-decoration:none;">
                    <i class="bi bi-arrow-left me-1"></i>Cancelar y volver
                </a>
            </div>
        </div>
    </div>

</div></div></div></div>
