@model IEnumerable<HuellitasFelices.Models.Consulta>
@using HuellitasFelices.ViewModels
@{ ViewData["Title"] = "Consultas"; var p = ViewBag.Paginacion as PaginacionViewModel; }

<div class="page-header">
    <div class="container">
        <nav aria-label="breadcrumb"><ol class="breadcrumb mb-2">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Inicio</a></li>
            <li class="breadcrumb-item active">Consultas</li>
        </ol></nav>
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1><i class="bi bi-clipboard2-pulse-fill me-2"></i>Consultas</h1>
                <small style="opacity:.8">@(p?.TotalRegistros.ToString("N0") ?? "–") consultas registradas</small>
            </div>
            <a asp-action="Create" class="btn-hero-primary"><i class="bi bi-plus-lg"></i> Nueva consulta</a>
        </div>
    </div>
</div>

<div class="content-wrapper"><div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-3">
        <form method="get" class="search-bar">
            <input type="text" name="busqueda" value="@p?.Busqueda" class="form-control" placeholder="Buscar por motivo…" />
            <button type="submit" class="btn-search"><i class="bi bi-search"></i></button>
            @if (!string.IsNullOrEmpty(p?.Busqueda)) { <a asp-action="Index" class="btn btn-sm btn-outline-secondary rounded-3"><i class="bi bi-x-lg"></i></a> }
        </form>
        @if (p != null) { <small class="text-muted">Página @p.PaginaActual de @p.TotalPaginas</small> }
    </div>

    <div class="hf-card"><div class="table-responsive">
        <table class="hf-table">
            <thead><tr>
                <th>Motivo</th><th>Diagnóstico</th><th>Costo</th><th>Mascota</th><th>Fecha</th><th style="width:130px">Acciones</th>
            </tr></thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.Motivo</strong></td>
                        <td style="font-size:.88rem; color:var(--text-soft)">@item.Diagnostico</td>
                        <td><span class="badge-success-hf">$@item.Costo.ToString("N2")</span></td>
                        <td style="font-size:.88rem">@item.Mascota?.Especie</td>
                        <td style="font-size:.85rem; color:var(--text-soft)">@item.FechaConsulta.ToLocalTime().ToString("dd/MM/yyyy")</td>
                        <td><div class="d-flex gap-1">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn-action btn-edit"><i class="bi bi-pencil"></i></a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn-action btn-view"><i class="bi bi-eye"></i></a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn-action btn-delete"><i class="bi bi-trash"></i></a>
                        </div></td>
                    </tr>
                }
            </tbody>
        </table>
    </div></div>
    @if (p != null) { @await Html.PartialAsync("_Paginacion", p) }
</div></div>
